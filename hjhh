local CoreGui = game:GetService("CoreGui")
local old = CoreGui:FindFirstChild("Rayfield")
if old then
    old:Destroy()
end

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local player = Players.LocalPlayer
local Remotes = ReplicatedStorage:WaitForChild("Remotes")
local SellGoldenPotatoes = Remotes:WaitForChild("SellGoldenPotatoes")
local PerformClick = Remotes:WaitForChild("PerformClick")
local PerformAscension = Remotes:WaitForChild("PerformAscension")
local PerformPrestige = Remotes:WaitForChild("PerformPrestige")
local PurchaseClickUpgrade = Remotes:WaitForChild("PurchaseClickUpgrade")
local PurchaseGenerator = Remotes:WaitForChild("PurchaseGenerator")
local DeleteGenerator = Remotes:WaitForChild("DeleteGenerator")
local PurchaseShopPotato = Remotes:WaitForChild("PurchaseShopPotato")

local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/UI-Interface/CustomFIeld/main/RayField.lua'))()
local Window = Rayfield:CreateWindow({
   Name = "Potato Script V3",
   LoadingTitle = "Idle Potato Game",
   LoadingSubtitle = "by Totemoflol",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "TatoHubConfig"
   },
   KeySystem = false
})

local SellTab = Window:CreateTab("Sell", 4483362458)
local AutoTab = Window:CreateTab("Auto", 4483362458)
local RebirthTab = Window:CreateTab("Rebirths", 4483362458)
local MacroTab = Window:CreateTab("Macro", 4483362458)
local MiscTab = Window:CreateTab("Misc", 4483362458)
local ShopTab = Window:CreateTab("Shop", 4483362458)

local Amount = 1
local Delay = 1
local Running = false
local Label = SellTab:CreateLabel("Auto Sell")
local AmountInput = SellTab:CreateInput({
   Name = "Amount Of Gold Potatoes",
   PlaceholderText = "Enter Amount",
   RemoveTextAfterFocusLost = false,
   Callback = function(Value)
      Amount = tonumber(Value) or 0
   end,
})
local DelayInput = SellTab:CreateInput({
   Name = "Delay (seconds)",
   PlaceholderText = "Enter Delay",
   RemoveTextAfterFocusLost = false,
   Callback = function(Value)
      Delay = tonumber(Value) or 0
   end,
})
local AutoSellToggle = SellTab:CreateToggle({
   Name = "Auto Sell",
   CurrentValue = false,
   Flag = "AutoSellToggle",
   Callback = function(Value)
      Running = Value

      if Running then
         task.spawn(function()
            while Running do
               SellGoldenPotatoes:FireServer(Amount)

               task.wait(Delay)
            end
         end)
      end
   end,
})

local Section = SellTab:CreateSection("Auto Sell Golden Potatoes")

local selling = false

local SellAllToggle = SellTab:CreateToggle({
    Name = "Auto Sell Golden Potatoes",
    CurrentValue = false,
    Flag = "AutoSellGolden",
    Callback = function(state)
        selling = state
    end,
})

task.spawn(function()
    local gui = player.PlayerGui:WaitForChild("PotatoGameGUI")
        .Background.ClickerArea.ClickerContainer.CurrencyFrame
    local goldLabel = gui:WaitForChild("GoldenRow"):WaitForChild("GoldenCount")

    while true do
        if selling then
            local gold = tonumber(goldLabel.Text) or 0
            if gold > 0 then
                SellGoldenPotatoes:FireServer(gold)
            end

        end
        task.wait(0.15)
    end
end)

local AutoClickLabel = AutoTab:CreateLabel("Auto Clicker")
local Clicking = false
local AutoClickToggle = AutoTab:CreateToggle({
    Name = "Auto Click (0.02s)",
    CurrentValue = false,
    Flag = "AutoClickToggle",
    Callback = function(Clicks)
        Clicking = Clicks

        task.spawn(function()
            while Clicking do
                PerformClick:FireServer()

                task.wait(0.005)
            end
        end)
    end,
})

local PrestigeSection = RebirthTab:CreateSection("Ascension")
local AscendLabel = RebirthTab:CreateLabel("Ascension Upgrades")
local AbundanceAscend = false
local AscendToggle = RebirthTab:CreateToggle({
    Name = "Abundance Ascension",
    CurrentValue = false,
    Flag = "AutoAscend",
    Callback = function(abundance)
        AbundanceAscend = abundance
        if AbundanceAscend then
            task.spawn(function()
                while AbundanceAscend do
                    PerformAscension:FireServer("abundance")
                    task.wait(60)
                end
            end)
        end
    end,
})

local PrestigeAscend = false

local PrestigeAscendToggle = RebirthTab:CreateToggle({
   Name = "Prestige Ascension",
   CurrentValue = false,
   Flag = "PrestigeAscension",
   Callback = function(PrestigeA)
      PrestigeAscend = PrestigeA

      if PrestigeAscend then
         task.spawn(function()
            while PrestigeAscend do
               PerformAscension:FireServer("prestige")

               task.wait(60)
            end
         end)
      end
   end,
})

local ThriftAscend = false
local ThriftyAscendToggle = RebirthTab:CreateToggle({
   Name = "Thrifty Ascension",
   CurrentValue = false,
   Flag = "ThriftyAscension",
   Callback = function(ThriftyAscend)
      ThriftAscend = ThriftyAscend
      if ThriftAscend then
         task.spawn(function()
            while ThriftAscend do
               PerformAscension:FireServer("thrifty")
               task.wait(60)
            end
         end)
      end
   end,
})

local Divider = RebirthTab:CreateSection("Prestige")
local PrestigeLabel = RebirthTab:CreateLabel("Auto Prestige")
local Prestiging = false
local AutoPrestigeToggle = RebirthTab:CreateToggle({
    Name = "Auto Prestige (32s)",
    CurrentValue = false,
    Flag = "AutoPrestigeToggle",
    Callback = function(APrestige)
        Prestiging = APrestige
        if Prestiging then
            task.spawn(function()
                while Prestiging do
                    PerformPrestige:FireServer()

                    task.wait(32)
                end
            end)
        end
    end,
})

local Section = MacroTab:CreateSection("Prestige Macro 15-30")
local StableMacroLabel = MacroTab:CreateLabel("Stable Macro")

local MPrestige = PerformPrestige
local MGoldenSell = SellGoldenPotatoes
local MWisdom = PurchaseClickUpgrade
local MEnergy = PurchaseClickUpgrade
local MOmni = PurchaseClickUpgrade
local MHarvest = PurchaseClickUpgrade
local MGalactic = PurchaseClickUpgrade

local macroing = false
local MacroV1Toggle = MacroTab:CreateToggle({
    Name = "Macro V1",
    CurrentValue = false,
    Flag = "MacroV1",
    Callback = function(MacroV1)
        macroing = MacroV1

        task.spawn(function()
            while macroing do
                MPrestige:FireServer()
                task.wait(5)
                  
                MGoldenSell:FireServer(200000)
                task.wait(0.5)
                  
                MWisdom:FireServer("grandfathers_wisdom")
                task.wait(0.5)
                  
                MGoldenSell:FireServer(1000000)
                task.wait(1)

                MWisdom:FireServer("grandfathers_wisdom")
                task.wait(2)

                MGoldenSell:FireServer(28000000)
                task.wait(1)

                MEnergy:FireServer("infinite_energy")
                task.wait(1)
                  
                MGoldenSell:FireServer(100000000)
                MEnergy:FireServer("infinite_energy")
                task.wait(3)
               
                MGoldenSell:FireServer(300000000)
                MEnergy:FireServer("infinite_energy")
                task.wait(3)

                MGoldenSell:FireServer(1000000000)
                MOmni:FireServer("omnipotato_blessing")
                task.wait(4)
                  
                MGoldenSell:FireServer(1800000000)
                MOmni:FireServer("omnipotato_blessing")
                task.wait(8)

                MGoldenSell:FireServer(14000000000)
                task.wait(1)
            end
        end)
    end,
})

local ksmacro = false

local MacroV1Toggle = MacroTab:CreateToggle({
    Name = "KS Macro V1",
    CurrentValue = false,
    Flag = "KSMacroV1",
    Callback = function(KSMacro1)
        ksmacro = KSMacro1

        task.spawn(function()
            while ksmacro do
                MPrestige:FireServer()
                task.wait(5)
                  
                MGoldenSell:FireServer(200000)
                task.wait(0.5)
                  
                MWisdom:FireServer("grandfathers_wisdom")
                task.wait(0.5)
                  
                MGoldenSell:FireServer(1000000)
                task.wait(1)

                MWisdom:FireServer("grandfathers_wisdom")
                task.wait(2)

                MGoldenSell:FireServer(28000000)
                task.wait(1)

                MEnergy:FireServer("infinite_energy")
                task.wait(1)
                  
                MGoldenSell:FireServer(100000000)
                MEnergy:FireServer("infinite_energy")
                task.wait(3)
               
                MGoldenSell:FireServer(300000000)
                MEnergy:FireServer("infinite_energy")
                task.wait(3)

                MGoldenSell:FireServer(1000000000)
                MOmni:FireServer("omnipotato_blessing")
                task.wait(4)
                  
                MGoldenSell:FireServer(1800000000)
                MOmni:FireServer("omnipotato_blessing")
                task.wait(8)

                MGoldenSell:FireServer(8000000000)
                task.wait(1)
            end
        end)
    end,
})

local SuperHumanMacroDivider = MacroTab:CreateSection("SuperHuman Macro")
local SuperHumanMacroLabel = MacroTab:CreateLabel("SuperHuman Macro")

local superhumanmacro = false

local SuperMacroV1 = {
    {"prestige", nil, 2.6},

    {"sell", 1_000_000, 0.05},
    {"wisdom", nil, 0.8},

    {"sell", 428_700, 0.05},
    {"wisdom", nil, 1.9},

    {"sell", 140_000_000, 0.05},
    {"energy", nil, 0.3},

    {"sell", 150_000_000, 0},
    {"energy", nil, 0.6},

    {"sell", 300_000_000, 0},
    {"energy", nil, 1},

    {"sell", 2_870_000_000, 0},
    {"omni", nil, 1},

    {"sell", 4_000_000_000, 0},
    {"omni", nil, 1},

    {"sell", 8_000_000_000, 0},
    {"harvest", nil, 0.6},

    {"sell", 27_830_000_000, 0},
    {"harvest", nil, 0.05},

    {"galactic", nil, 0.5},

    {"sell", 40_670_000_000, 0},
    {"galactic", nil, 20.5},

    {"sell", 3_000_000_000_000, 0.5}
}

local MacroV1Toggle = MacroTab:CreateToggle({
    Name = "SuperHumanMacro V1",
    CurrentValue = false,
    Flag = "SuperHumanMacroV1",

    Callback = function(superhumanmacrov1)
        superhumanmacro = superhumanmacrov1
        task.spawn(function()
            while superhumanmacro do

                for _,step in ipairs(SuperMacroV1) do
                    if not superhumanmacro then break end

                    local action,value,delay = unpack(step)

                    if action == "prestige" then
                        MPrestige:FireServer()

                    elseif action == "sell" then
                        MGoldenSell:FireServer(value)

                    elseif action == "wisdom" then
                        MWisdom:FireServer("grandfathers_wisdom")

                    elseif action == "energy" then
                        MEnergy:FireServer("infinite_energy")

                    elseif action == "omni" then
                        MOmni:FireServer("omnipotato_blessing")

                    elseif action == "harvest" then
                        MHarvest:FireServer("transcendent_harvest")

                    elseif action == "galactic" then
                        MGalactic:FireServer("galactic_harvest")
                    end

                    task.wait(delay)
                end

            end
        end)
    end,
})


local GeneratorMacroDivider = MacroTab:CreateSection("Generator Macro")
local GeneratorMacroLabel = MacroTab:CreateLabel("Generator Macro")

local generatorfarm = false
local GeneratorMacroV1Toggle = MacroTab:CreateToggle({
    Name = "Generator Farm V1",
    CurrentValue = false,
    Flag = "GeneratorFarmV1",
    Callback = function(GenFarmV1)
        generatorfarm = GenFarmV1

        task.spawn(function()
            while generatorfarm do
                PerformPrestige:FireServer()
                task.wait(1)
                  
                SellGoldenPotatoes:FireServer(4_400_000)
                task.wait(0.05)
                  
                PurchaseGenerator:FireServer("dimensional_mirror")
                task.wait(0.5)
                  
                SellGoldenPotatoes:FireServer(460_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("temporal_harvester")
                task.wait(3)

                SellGoldenPotatoes:FireServer(100_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("superfactory_number_67")
                task.wait(1.8)

                SellGoldenPotatoes:FireServer(1_200_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("potato_nexus")
                task.wait(3.4)

                SellGoldenPotatoes:FireServer(20_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("omnipotato")
                task.wait(0.8)

                SellGoldenPotatoes:FireServer(30_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("omnipotato")
                task.wait(3.8)

                SellGoldenPotatoes:FireServer(300_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("double_omnipotato")
                task.wait(2)

                SellGoldenPotatoes:FireServer(800_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("double_omnipotato")
                task.wait(4)

                SellGoldenPotatoes:FireServer(5_000_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("infinite_omnipotato")
                task.wait(1)

                SellGoldenPotatoes:FireServer(8_000_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("infinite_omnipotato")
                task.wait(3.4)

                SellGoldenPotatoes:FireServer(50_000_000_000_000_000)
                task.wait(0.05)

                PurchaseGenerator:FireServer("potato_infinite_universe")
                task.wait(7)

                SellGoldenPotatoes:FireServer(2_000_000_000_000_000_000)
                task.wait(0.10)

            end
        end)
    end,
})

local NotificationContainer = player.PlayerGui:WaitForChild("PotatoGameGUI"):WaitForChild("NotificationContainer")
local NoNotifToggle = MiscTab:CreateToggle({
   Name = "Disable Notifications",
   CurrentValue = false,
   Flag = "DisableNotifications",
   Callback = function(DisableNotifications)
		NotificationContainer.Visible = not DisableNotifications
   end,
})

local GeneratorFarm = false

local GeneratorFarmToggle = MiscTab:CreateToggle({
   Name = "Auto Farm Generator",
   CurrentValue = false,
   Flag = "GeneratorFarm",
   Callback = function(GFarm)
      GeneratorFarm = GFarm

      task.spawn(function()
         while GeneratorFarm do
            PurchaseGenerator:FireServer("potato_seedling")
            DeleteGenerator:FireServer("potato_seedling")
            task.wait(0.05)
         end
      end)
   end,
})

local autobuy = false
local ProductionAutoBuyToggle = ShopTab:CreateToggle({
   Name = "Production AutoBuy",
   CurrentValue = false,
   Flag = "ProductionAutoBuy",
   Callback = function(ProductionPotion)
      AutoBuyProductionPotion = ProductionPotion
      task.spawn(function()
         while AutoBuyProductionPotion do
            PurchaseShopPotato:FireServer("potion_production")
            task.wait(60)
         end
      end)
   end,
})

local autobuyluck = false
local LuckAutoBuyToggle = ShopTab:CreateToggle({
   Name = "Luck AutoBuy",
   CurrentValue = false,
   Flag = "LuckAutoBuy",
   Callback = function(LuckPotion)
      AutoBuyLuckPotion = LuckPotion
      task.spawn(function()
         while AutoBuyLuckPotion do
            PurchaseShopPotato:FireServer("potion_luck")
            task.wait(60)
         end
      end)
   end,
})

local autobuygolden = false
local GoldenAutoBuyToggle = ShopTab:CreateToggle({
   Name = "GoldenAutoBuy",
   CurrentValue = false,
   Flag = "GoldenAutoBuy",
   Callback = function(GoldenPotion)
      AutoBuyGoldenPotion = GoldenPotion
      task.spawn(function()
         while AutoBuyGoldenPotion do
            PurchaseShopPotato:FireServer("potion_golden")
            task.wait(60)
         end
      end)
   end,
})
local AutoBuyClickPotion = false
local ClickAutoBuyToggle = ShopTab:CreateToggle({
   Name = "Click AutoBuy",
   CurrentValue = false,
   Flag = "ClickAutoBuy",
   Callback = function(ClickPotion)
      AutoBuyClickPotion = ClickPotion
      task.spawn(function()
         while AutoBuyClickPotion do
            PurchaseShopPotato:FireServer("potion_click")
            task.wait(60)
         end
      end)
   end,
})

if getconnections then
    for _, connection in pairs(getconnections(player.Idled)) do
        if connection.Disable then
            connection:Disable()
        elseif connection.Disconnect then
            connection:Disconnect()
        end
    end
else
    player.Idled:Connect(function()
        VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end)
end

Rayfield:LoadConfiguration()
